package com.spritechase.ui{	import flash.display.MovieClip;	import flash.events.Event;		import com.spritechase.core.*;		public class UIScreen extends MovieClip	{		public function UIScreen()		{			trace('UIScreen()');			addEventListener(Event.ADDED_TO_STAGE, onAddedToStage, false, 0, true);		}				private function onAddedToStage(e:Event):void		{			removeEventListener(Event.ADDED_TO_STAGE, onAddedToStage);			init();		}				private function init():void		{			//subclasses perform content-specific stage initialization here		}				public function update(data:KioskData):void		{			//subclasses perform content-specific updates here		}				public function idle():void		{			//subclasses perform content-specific idling cleanup here		}	}	}